version: '3.8'

services:
  agent:
    build: .
    environment:
      # 运行时从宿主机环境变量注入
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_BASE_URL=${LLM_BASE_URL:-https://api.apiyi.com/v1}
    volumes:
      - ./data:/app/data
    # ports:
    #   - "8000:8000"
