delta_version: 0.3.0
change_id: deep-cleanup-main-batch5
change_type: batch5-archive-audit-and-fallback
status: in_progress
intent:
  summary: >
    Recheck the lowest-risk repair-side archive candidates, characterize their contracts,
    and only archive them if no hidden dependency remains. Current Batch 5 outcome is
    fallback-to-blocked because hidden rule and recovery-contract dependencies were found.
  non_goals:
    - Rewriting run_smoke_pipeline.py
    - Altering the keep-chain runtime contract
    - Modifying repair_loop.py, run_validation.py, or build_validation_set.py
    - Physically cleaning repo-root src/scripts
    - Reworking repair-side historical scripts into supported tools
assertions:
  authority_zone: main_worktree/scripts
  compat_zone: src/scripts
  keep_chain:
    - llm_ping
    - normalize_guard
    - translate_llm
    - qa_hard
    - rehydrate_export
    - smoke_verify
  minimal_irreversible_core:
    - scripts/run_smoke_pipeline.py
    - scripts/runtime_adapter.py
    - scripts/smoke_issue_logger.py
    - scripts/m4_3_collect_coverage.py
    - scripts/m4_4_decision.py
    - tests/test_smoke_verify.py
    - tests/test_qa_hard.py
    - tests/test_normalize_segmentation.py
planned_controls:
  - Recheck references for repair_loop_v2.py and repair_checkpoint_gaps.py before archiving.
  - Add characterization tests that preserve CLI and recovery expectations.
  - Revert to inventory-only status if any hidden dependency appears.
  - Update frozen-zone inventory and Batch 5 report so the fallback decision remains auditable.
status_map:
  repair_loop_v2.py: blocked after fallback
  repair_checkpoint_gaps.py: blocked after fallback
  repair_loop.py: blocked
  run_validation.py: blocked
  build_validation_set.py: blocked
  runtime_adapter.py: must-keep core
blocked_zones:
  - scripts/repair_loop.py
  - scripts/repair_loop_v2.py
  - scripts/repair_checkpoint_gaps.py
  - scripts/run_validation.py
  - scripts/build_validation_set.py
  - gate/**
  - stress/**
  - ../src/scripts/**
risk_controls:
  - Keep-chain scripts and blocked repair/validation surfaces remain untouched.
  - Archive actions must be rolled back immediately if hidden dependencies are discovered.
  - Governance artifacts must explain why the two candidates remain blocked after fallback.
  - authority report may remain WARN on runtime_adapter drift, but must not add required mirror drift.
  - M4 decision logic and smoke evidence generation remain unchanged.
